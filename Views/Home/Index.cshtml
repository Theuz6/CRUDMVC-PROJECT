@{
    ViewData["Title"] = "Página Inicial";
    var hora = DateTime.Now.Hour;
    var msg = "";
    if (hora > 12){
        msg = "boa tarde";
    } else if (hora>=18){
        msg = "boa noite";
    }else{
        msg = "bom dia"; 
    }
}

<div class="text-center">
    <h1 class="display-4">Seja Bem-Vindo(a), @msg!</h1>
    <h5>Sistema de cadastros de usuários</h5>
</div>
